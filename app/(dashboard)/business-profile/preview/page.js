import PreviewPage from "@/components/BusinessProfile/Preview";

export const metadata = () => {
	return {
		title: "Preview - Business Profile | Passpoint Go",
		description: "",
	};
};
const Preview = () => {
	return <PreviewPage />;
};

export default Preview;
